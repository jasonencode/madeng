<Window x:Class="MaDeng.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MaDeng" Width="260" Height="52"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent" Topmost="True"
        ShowInTaskbar="False" ResizeMode="NoResize"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        MouseDoubleClick="Window_MouseDoubleClick"
        Loaded="Window_Loaded">

    <Window.Resources>
        <ContextMenu x:Key="TrayMenu">
            <MenuItem Header="设置" Click="Settings_Click"/>
            <Separator/>
            <MenuItem Header="恢复Ready状态" Click="ResetStatus_Click"/>
            <MenuItem Header="还原位置" Click="ResetPosition_Click"/>
            <Separator/>
            <MenuItem Header="关于" Click="About_Click"/>
            <MenuItem Header="退出" Click="Exit_Click"/>
        </ContextMenu>
    </Window.Resources>

    <Border x:Name="MainBorder" CornerRadius="0,0,14,14"
            Background="#1E1E2E" Opacity="0.6"
            ContextMenu="{StaticResource TrayMenu}">
        <Border.Effect>
            <DropShadowEffect BlurRadius="8" Opacity="0.3" ShadowDepth="2"/>
        </Border.Effect>

        <StackPanel x:Name="LightsPanel" Orientation="Horizontal"
                    HorizontalAlignment="Center" VerticalAlignment="Center"
                    Margin="0,0,0,2">

            <!-- 绿灯 -->
            <StackPanel Margin="8,0">
                <Ellipse x:Name="GreenLightH" Width="24" Height="24">
                    <Ellipse.Effect>
                        <DropShadowEffect x:Name="GreenGlowH" Color="#16A34A" BlurRadius="0" Opacity="0"/>
                    </Ellipse.Effect>
                    <Ellipse.Fill>
                        <RadialGradientBrush>
                            <GradientStop x:Name="GreenInnerH" Color="#1A1A2E" Offset="0"/>
                            <GradientStop x:Name="GreenOuterH" Color="#1A1A2E" Offset="1"/>
                        </RadialGradientBrush>
                    </Ellipse.Fill>
                </Ellipse>
            </StackPanel>

            <!-- 黄灯 -->
            <StackPanel Margin="8,0">
                <Ellipse x:Name="YellowLightH" Width="24" Height="24">
                    <Ellipse.Effect>
                        <DropShadowEffect x:Name="YellowGlowH" Color="#EAB308" BlurRadius="0" Opacity="0"/>
                    </Ellipse.Effect>
                    <Ellipse.Fill>
                        <RadialGradientBrush>
                            <GradientStop x:Name="YellowInnerH" Color="#1A1A2E" Offset="0"/>
                            <GradientStop x:Name="YellowOuterH" Color="#1A1A2E" Offset="1"/>
                        </RadialGradientBrush>
                    </Ellipse.Fill>
                </Ellipse>
            </StackPanel>

            <!-- 红灯 -->
            <StackPanel Margin="8,0">
                <Ellipse x:Name="RedLightH" Width="24" Height="24">
                    <Ellipse.Effect>
                        <DropShadowEffect x:Name="RedGlowH" Color="#DC2626" BlurRadius="0" Opacity="0"/>
                    </Ellipse.Effect>
                    <Ellipse.Fill>
                        <RadialGradientBrush>
                            <GradientStop x:Name="RedInnerH" Color="#1A1A2E" Offset="0"/>
                            <GradientStop x:Name="RedOuterH" Color="#1A1A2E" Offset="1"/>
                        </RadialGradientBrush>
                    </Ellipse.Fill>
                </Ellipse>
            </StackPanel>

            <!-- 状态文本 -->
            <TextBlock x:Name="StatusTextH" Text="Ready"
                       Foreground="#E0E0E0" FontSize="14" FontWeight="Bold"
                       VerticalAlignment="Center" Margin="8,0,0,0"/>
        </StackPanel>
    </Border>
</Window>
